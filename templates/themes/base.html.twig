<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %} - GOS-AUTOMOBILE</title>
         <meta name="description" content="{% block description %}{% endblock %}">

        <meta name="robots" content="index, follow">
        <meta name="author" content="GOS-AUTOMOBILE">
        <meta name="publisher" content="GOS-AUTOMOBILE">

        {# Google font #}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

         {# Favicon #}
        <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96"/>
        <link rel="icon" type="image/svg+xml" href="/favicon.svg"/>
        <link rel="shortcut icon" href="/favicon.ico"/>
        <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png"/>
        <link rel="manifest" href="/site.webmanifest"/>
        <meta name="theme-color" content="#712cf9">

        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

        {# Font Awesome #}
        {% block fontAwesome %}{% endblock %}

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}

       

        {% block j_query %}{% endblock %}

        {% block specific_js %}{% endblock %}

        {% block knp_paginator_rel_links %}{% endblock %}
        {% endblock %}
    </head>
    <body class="body-visitor">
        {# Navbar #}

        {% set pathClient = app.request.pathInfo %} 

        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{path('app_visitor_welcome')}}">Gos-Automobile</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                     <a class="nav-link {{ pathClient == path('app_visitor_welcome') ? 'jd-active' : '' }}" {{  pathClient == path('app_visitor_welcome') ? 'aria-current="page"' : ''}} href="{{path('app_visitor_welcome')}}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_visitor_welcome')}}#about-me">Qui suis-je?</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ pathClient == path('app_visitor_prestations_index') ? 'jd-active' : '' }}" {{  pathClient == path('app_visitor_prestations_index') ? 'aria-current="page"' : ''}} href="{{path('app_visitor_prestations_index')}}">Prestations</a>
                </li>
                <li class="nav-item">
                    {# <a class="nav-link" href="{{path('app_visitor_contact')}}">Contact</a> #}
                    <a class="nav-link {{ pathClient == path('app_visitor_contact') ? 'jd-active' : '' }}" {{  pathClient == path('app_visitor_contact') ? 'aria-current="page"' : ''}} href="{{path('app_visitor_contact')}}">Contact</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="my-1 mx-lg-2 btn btn-outline-warning shadow" href="{{path('app_admin_home')}}">Espace admin</a>
                    </li>
                    {% endif %}

                     {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="my-1 mx-lg-2 btn btn-outline-warning shadow" href="{{path('app_user_home')}}">Espace utilisateur</a>
                    </li>
                {% endif %}
                    <li class="nav-item">
                        <a class="my-1 mx-lg-2 btn btn-warning shadow" href="{{path('app_logout')}}">Déconnexion</a>
                    </li>
                {% else %}
                <li class="nav-item">
                    <a class="my-1 mx-lg-2 btn btn-outline-warning shadow" href="{{path('app_login')}}">Connexion</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

        {# Main #}
        <main>
            {% block main %}{% endblock %}
        </main>

        {# Footer #}
        <footer class="bg-primary text-white">
            <div
                class="container py-4">

               
                <div
                    class="d-flex flex-column flex-sm-row justify-content-between align-items-center">

                   
                    <div class="mb-3 mb-sm-0">
                        <span class="text-white-50">©
                            {{ "now"|date("Y") }}
                            GOS Automobile. Tous droits réservés.</span>
                    </div>

                  
                    <div>
                        <ul class="list-unstyled d-flex mb-0">
                            <li class="ms-3">
                                <a class="link-light fs-4" href="https://www.facebook.com/Vignonet/" aria-label="Facebook" target="_blank" rel="noopener noreferrer">
                                    <i class="fab fa-facebook-f"></i>
                                </a>
                            </li>
                            <li class="ms-3">
                                <a class="link-light fs-4" href="https://www.instagram.com/gos.automobile/" aria-label="Instagram" target="_blank" rel="noopener noreferrer">
                                    <i class="fab fa-instagram"></i>
                                </a>
                            </li>
                            <li class="ms-3">
                                <a class="link-light fs-4" href="https://wa.me/33761793691?text=Bonjour%2C+j%27ai+vu+votre+site." aria-label="WhatsApp" target="_blank" rel="noopener noreferrer">
                                    <i class="fab fa-whatsapp"></i>
                                </a>
                            </li>
                        </ul>
                    </div>

                </div>
            </div>

        </footer>

    </body>
</html>
